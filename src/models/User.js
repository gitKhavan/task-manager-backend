const db = require("../../database/connect");

class User{
    constructor({user_id, email, password}){
        this.user_id = userId;
        this.email = email;
        this.password = password;
    }

}